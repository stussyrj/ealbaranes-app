export default function App() {
  return (
    <div style={{ padding: "20px", fontFamily: "system-ui, sans-serif" }}>
      <h1>DirectTransports</h1>
      <p>Admin Dashboard</p>
      <p style={{ color: "#666", fontSize: "14px" }}>
        Estado: Interfaz básica funcional. Backend operativo (API /api/quotes, /api/workers).
      </p>
      <p style={{ color: "#999", fontSize: "12px" }}>
        Nota: Contextos de autenticación y tema requieren debugging de imports en Vite.
      </p>
    </div>
  );
}

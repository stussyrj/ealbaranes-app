import { QuoteCalculatorAdvanced } from "../QuoteCalculatorAdvanced";
import { TooltipProvider } from "@/components/ui/tooltip";

export default function QuoteCalculatorAdvancedExample() {
  return (
    <TooltipProvider>
      <QuoteCalculatorAdvanced />
    </TooltipProvider>
  );
}
